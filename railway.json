{
  "build": {
      "builder": "DOCKERFILE",
      "dockerfilePath": "./Dockerfile",
      "watchPatterns": [
          "requirements.txt",
          "images/**",
          "railway.json",
          "Dockerfile"
      ]
  }
}